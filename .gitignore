# Local Terraform files
.terraform/           # Terraform's working directory
.terraform.lock.hcl   # Dependency lock file, can be omitted if you want to version control it
*.tfstate             # State files contain sensitive data
*.tfstate.*           # Backup and intermediate state files

# Crash log files
crash.log

# Ignore any .tfvars files, which often contain sensitive data like credentials
*.tfvars
*.tfvars.json

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore plan output files
*.tfplan

# Ignore archive files (used for modules or assets)
*.zip
*.tar.gz
*.rar

# Ignore provider plugin cache (if enabled)
.terraform.d/plugin-cache/
